events {}

http {
    server {
        listen 80;
        server_name _;

        location /health {
            deny all;
            return 404;
        }

        location / {
            proxy_pass http://web:5500;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}